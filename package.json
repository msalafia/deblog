{
  "name": "deblog",
  "version": "1.0.0",
  "description": "A simple console logger for debug experiments and flexible logging configuration",
  "main": "dist/deblog.cjs.js",
  "browser": "dist/deblog.esm.js",
  "types": "types/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && rm -rf types && npx tsc && npx rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marco Salafia",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^2.74.1",
    "typescript": "^4.6.4"
  }
}